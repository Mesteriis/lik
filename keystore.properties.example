# Copy to keystore.properties only when configuring a signed release.
# Paths can be absolute or relative to the repository root.
storeFile=/absolute/path/to/lik-release.jks
storePassword=replace-with-store-password
keyAlias=lik
keyPassword=replace-with-key-password
